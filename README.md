# Dispatch 

Dispatch is a process manager that lets you control which CPU cores your running tasks are pinned to. Instead of letting your OS scatter everything across all available cores, you can assign specific processes to specific cores — keeping background tasks out of the way, letting idle cores sleep, and giving your most important work dedicated resour
